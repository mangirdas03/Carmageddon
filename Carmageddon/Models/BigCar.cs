namespace Carmageddon.Forms.Models
{
    public class BigCar : Car
    {
        public BigCar(int health, int length)
        {
            this.Health = health;
            this.Length = length;
        }
    }
}
